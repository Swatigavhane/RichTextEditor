import type { TextSpan } from './types';

export const DEFAULT_BLOCK_ID = 'block-1';
export const BLOCK_SEPARATOR_LENGTH = 1;

export const LEFT_BLOCK_SUFFIX = 'left';
export const RIGHT_BLOCK_SUFFIX = 'right';

export const EMPTY_TEXT_SPAN: TextSpan = { text: '', marks: [] };
